import { answerNotification } from 'ember-message-board/helpers/answer-notification';
import { module, test } from 'qunit';

module('Unit | Helper | answer notification');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerNotification([42]);
  assert.ok(result);
});
